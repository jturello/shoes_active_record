
<a href="/stores/new">Add a Store</a>
<br>
<a href="/shoes">Add a Shoe Brand</a>

<hr>

<% if @stores.size > 0 %>
<h3><%= @sub_header_instructions %></h3>
    <ul>
      <% @stores.each do |store| %>
      <li><a href="/stores/<%= store.id %>"><%= store.name %></a></li>
      <% end %>
    </ul>
<% end %>
